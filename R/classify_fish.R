classify_fish
#'@param fish name (salmon, tuna, steelhead, cod, shark)

classify_fish = function(fish_name) {
  result = case_when(fish_name = 50 )

    
    return(result)
    }


Store as a vector of numbers, then calculate  max and min 

1. the most common fish,
2. the rarest fish
3. the total number of fish (count?)
